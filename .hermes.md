# JodTang — กฎของโปรเจกต์นี้ (อ่านก่อนแตะโค้ด)

เว็บแอปจดบันทึกรายรับรายจ่าย + AI วิเคราะห์ · ใช้เองและให้เพื่อนใช้
สเปกฉบับเต็ม: `docs/PLAN.md` (แหล่งความจริงของขอบเขต) — อ่านก่อนเริ่มงานทุกครั้ง

## ภาษา
- เอกสาร คอมเมนต์ commit message: **ไทย** · ชื่อตัวแปร/ฟังก์ชัน/ไฟล์: **อังกฤษ**
- เขียนคอมเมนต์อธิบาย "ทำไม" ไม่ใช่ "ทำอะไร" (โค้ดบอก "ทำอะไร" อยู่แล้ว)

## โปรเจกต์นี้เขียนใหม่ทั้งหมด — ห้ามยกของเก่ามาใช้
- **ห้ามคัดลอกโค้ด/ไฟล์/schema/UI จากโปรเจกต์ก่อนหน้า** ที่อยู่ในเครื่องนี้ (`~/work/JodJai` และงาน PX-Pos) แม้จะเปิดอ่านได้ก็ตาม — เขียนใหม่ให้ตรงกับสเปกปัจจุบัน
- อ่านของเก่าได้เพื่อ "บทเรียน" เท่านั้น (เช่น จุดที่เคยพลาด) และถ้าจะอ้าง ให้เขียนเหตุผลในคอมเมนต์การ์ดแล้วตัดสินใจใหม่ด้วยข้อมูลวันนี้ — เวอร์ชันและ API เปลี่ยนไปแล้วหลายตัว: Next 16.3.5 · React 19.3 · Tailwind 4.3 · คีย์ Supabase ชุดใหม่ (`sb_publishable_…` / `sb_secret_…`)
- เอกสารของโปรเจกต์นี้คือ `docs/PLAN.md` (ขอบเขต) และ `docs/DESIGN.md` (ดีไซน์ — จะตามมา) เท่านั้นที่เป็นแหล่งความจริง

## ความลับของผู้ใช้ — ห้ามอ่านไฟล์ env
- **ห้ามอ่าน/เปิด/print `.env.local` หรือไฟล์ `.env*` ใด ๆ ยกเว้น `.env.example`** ไม่ว่าจะผ่าน `read_file`, `cat`, `grep`, `sed`, `head`, `diff`, `git show` หรือเครื่องมืออื่น · ถ้าเผลอเห็นค่ามา ให้หยุด อย่าคัดลอกลงคอมเมนต์/สรุป/commit และแจ้ง lead ทันที
- ตรวจว่ามีไฟล์อยู่ได้โดยไม่เปิดเนื้อหา: `test -f .env.local && echo present`
- **ห้ามเขียน/แก้ `.env.local`** — เจ้าของโปรเจกต์ (yoru) ใส่ค่าเองเท่านั้น · งานที่ต้องใช้ค่า env ให้ยึดชื่อตัวแปรจาก `.env.example` และต้องรันได้เมื่อ env ว่าง
- ห้าม commit ไฟล์ env ทุกชนิด (`.gitignore` กันไว้แล้ว) — ถ้าเห็นความเสี่ยงว่าจะหลุด ให้ block ทันที

## ห้ามเขียนไฟล์นอก workspace ของการ์ด (บทเรียนจริง 2026-09-16)

- **ทุกการเขียนไฟล์ต้องใช้พาธใต้ `$HERMES_KANBAN_WORKSPACE`** (หรือ absolute path ที่ขึ้นต้นด้วย workspace นั้น) — ห้ามเขียนด้วยพาธที่ไปลง repo หลัก `/home/yoro/work/JodTang/...` เพราะจะทำให้ main สกปรกและ merge ของคนอื่นล้มเหลว (เกิดขึ้นจริง: การ์ดหน้าแรกเขียนทับ main จน merge motion ไม่ได้)
- เริ่มงานทุกครั้ง: `cd $HERMES_KANBAN_WORKSPACE && pwd` แล้วยืนยันว่าไม่ใช่ repo หลัก
- ก่อนปิดการ์ด: `git -C /home/yoro/work/JodTang status --short` ต้องว่าง — ถ้ามีไฟล์ของคุณโผล่ที่นั่น แปลว่ายังทำผิด ให้ย้ายเข้า worktree แล้วรายงานในคอมเมนต์

## กติกาที่ห้ามละเมิด
1. **เงินเก็บเป็น bigint หน่วยสตางค์เสมอ** — ห้าม float/number/numeric กับเงินแม้แต่ที่เดียว
2. **RLS เปิดทุกตาราง** และ policy ต้องอ้าง `auth.uid()` — แตะ policy ทุกครั้งต้องมีเทสต์เชิงลบ (A อ่านของ B ไม่ได้) แนบหลักฐาน
3. ลบรายการ = soft delete (`deleted_at`) ไม่ใช่ DELETE · ทุก query ต้องกรอง
4. **ห้าม commit secret** — คีย์ทุกตัวอยู่ใน `.env.local` (gitignore) · ไฟล์ที่ commit ได้คือ `.env.example` ที่มีแต่ชื่อตัวแปร
5. งานที่แตะเงินหรือสิทธิ์ ต้องผ่าน review → qa ก่อนปิดการ์ด

## กติกาการทำงานของทีม
- ทำงานใน **worktree ของการ์ดตัวเองเท่านั้น** (`$HERMES_KANBAN_WORKSPACE`) · one writer per file
- **ห้าม push** — เครื่องนี้ไม่มี credential · commit ใน worktree แล้ว lead รวมเข้า main หลัง qa ผ่าน
- **ปิดการ์ดได้ต่อเมื่อรันของจริงแล้ว** และแนบ output จริง (build/เทสต์/probe) — คำบอกเล่าไม่นับ
- รันไม่ผ่านซ้ำสองครั้ง: block การ์ดพร้อมข้อความ error จริง อย่าวนแก้
- ไม่มี Docker บนเครื่องนี้ → `supabase start` ใช้ไม่ได้ · เทสต์ใช้ PGlite (`@electric-sql/pglite`) · migration เป็น SQL ใต้ `supabase/migrations/`

## คำสั่งที่ใช้บ่อย (จะเติมเมื่อ scaffold เสร็จ)
- `npm run dev` / `npm run build` · `npm test` (node --test + PGlite) · `npm run lint`
- `npx supabase migration new <ชื่อ>` · `npx supabase gen types typescript --project-id <id> > src/lib/database.types.ts`
